# docs/prompt TODO

`docs/prompt/TODO.md` は、`prompt` 領域でこれから着手する未完了タスクだけを置く作業キューです。
完了済みの系列追加や過去ログは [prompt-history.md](./prompt-history.md) に退避します。

## 現在の優先タスク

- [ ] `prompt-gen` の可変入力 `commitId` について、SHA らしい文字だけを許可する制約を入れるか判断し、必要なら実装する
- [ ] `prompt-gen` の可変入力 `subject` について、命令ではなく文字列値として扱う明示文を本文へ加えるか判断し、必要なら実装する
- [ ] `prompt-gen` の「共有リンクをコピー」ボタンの SVG アイコンを、より一般的で視認性の高い link / chain 表現へ改善する
- [ ] `prompt-gen` で、ユーザー定義プロンプト JSON の読み込み、`localStorage` 保存、反映、削除を扱えるようにする
- [ ] `S603-001` の枝番が `A603` と対応づけにくい問題を整理し、必要なら別枝番へ移す
- [ ] `A751: 要求達成に必要な範囲だけを変更` の内容を、出力オプションの `最小差分` に統合または反映すべきか検討する

## 検討中テーマ

- [ ] `A854 companion-prompt` の品質改善方針を整理する
  - 伴走フローの状態遷移
  - 各段階の終了条件
  - 初回応答の型
  - 質問過多を避ける優先順位
  - 仮 subject / 仮構成 / 仮説の扱い
  - 情報不足時の暫定出力条件
  - `Instructions` / `Rules` / `Examples` への反映方針

## 運用メモ

- 完了済み項目はこのファイルへ残し続けず、必要なら [prompt-history.md](./prompt-history.md) へ移します。
- 系列追加の大量ログは TODO ではなく履歴として管理します。
